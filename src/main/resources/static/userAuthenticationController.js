function authenticate(){
	alert("authentocate JS called");
	window.location.replace("/products");
	/*$.ajax({
        url: "http://localhost:8080/userAuthenticate"
 
    }).then(function(data) {
       	alert(data);
    });*/
}